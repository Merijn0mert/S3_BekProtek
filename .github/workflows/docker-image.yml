name: Docker Image CI

on:
  push:
    branches: [ "Actions" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        cd Application/Backend/bekprotek
        wsl docker build . --file Dockerfile --tag bekprotek-server:latest
